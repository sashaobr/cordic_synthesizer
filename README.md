This is an FPGA-based audio synthesizer based on the CORDIC algorithm (it's sick, look it up !)
Currently a WiP, with a simple Python implementation using fixed point numbers serving as a refresher for me :) 
